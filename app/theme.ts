// app/theme.ts

import { createTheme } from "@mantine/core";

// Theme configuration
const mantineTheme = createTheme({
  primaryColor: "blue",
  defaultRadius: "md",
});

export default mantineTheme;
