/* Redesigned PromptTypeSelector.module.css for modern, clean, and consistent cards */

.typeSelectorWrapper {
  margin: 2.5rem 0 2rem 0;
  text-align: center;
}

.typeSelectorTitle {
  color: var(--mantine-color-blue-7, #1864ab);
  font-weight: 800;
  margin-bottom: 2rem;
  font-size: 1.3rem;
  letter-spacing: 0.01em;
}

.typeCard,
.typeCardSelected {
  width: 240px;
  min-width: 240px;
  max-width: 240px;
  height: 340px;
  min-height: 340px;
  max-height: 340px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: flex-start;
  background: var(--mantine-color-white, #fff);
  border-radius: 18px;
  box-shadow: 0 2px 16px 0 rgba(60, 60, 60, 0.07);
  border: 1.5px solid var(--mantine-color-gray-2, #e9ecef);
  padding: 2.2rem 1.2rem 1.5rem 1.2rem;
  cursor: pointer;
  transition: box-shadow 0.22s, border 0.22s, transform 0.18s;
  position: relative;
  outline: none;
  box-sizing: border-box;
  gap: 0.7rem;
}

.typeCard:hover {
  border: 2px solid var(--mantine-color-blue-5, #339af0);
  box-shadow: 0 6px 32px 0 rgba(34, 139, 230, 0.13);
  transform: translateY(-4px) scale(1.025);
  background: var(--mantine-color-blue-0, #e7f5ff);
}

.typeCardSelected {
  border: 2.5px solid var(--mantine-color-blue-6, #228be6);
  box-shadow: 0 8px 32px 0 rgba(34, 139, 230, 0.18);
  background: linear-gradient(135deg, #e7f5ff 60%, #f8fafc 100%);
  transform: translateY(-6px) scale(1.04);
}

.typeIcon {
  margin-bottom: 0.7rem;
  filter: drop-shadow(0 2px 8px #e7f5ff);
  font-size: 3.2rem;
}

.typeTitle {
  width: 100%;
  text-align: center;
  font-weight: 700;
  font-size: 1.18rem;
  color: var(--mantine-color-blue-7, #1c7ed6);
  margin-bottom: 0.2rem;
  letter-spacing: 0.01em;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  line-clamp: 2;
  max-height: 2.8em;
  min-height: 2.8em;
  white-space: normal;
  overflow: hidden;
  text-overflow: ellipsis;
}

.typeDescription {
  width: 100%;
  text-align: center;
  color: var(--mantine-color-gray-6, #868e96);
  font-size: 1.04rem;
  font-weight: 400;
  margin-top: 0.2rem;
  line-height: 1.5;
  min-height: 3.2em;
  max-height: 3.2em;
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  line-clamp: 2;
  white-space: normal;
}
/* PromptTypeSelector.module.css - Modern, clean, fancy card styles for prompt type selection */

.typeSelectorWrapper {
  margin: 2.5rem 0 2rem 0;
  text-align: center;
}

.typeSelectorTitle {
  color: var(--mantine-color-blue-7, #1864ab);
  font-weight: 800;
  margin-bottom: 2rem;
  font-size: 1.3rem;
  letter-spacing: 0.01em;
}

.typeCard {
  border: 1.5px solid var(--mantine-color-gray-2, #e9ecef);
  background: var(--mantine-color-white, #fff);
  border-radius: 18px;
  box-shadow: 0 2px 16px 0 rgba(60, 60, 60, 0.06);
  transition: box-shadow 0.22s, border 0.22s, transform 0.18s;
  cursor: pointer;
  min-width: 220px;
  max-width: 260px;
  min-height: 260px;
  height: 100%;
  padding: 2.2rem 1.2rem 1.5rem 1.2rem;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.7rem;
  position: relative;
  outline: none;
  box-sizing: border-box;
}
.typeCard:hover {
  border: 2px solid var(--mantine-color-blue-5, #339af0);
  box-shadow: 0 6px 32px 0 rgba(34, 139, 230, 0.13);
  transform: translateY(-4px) scale(1.025);
  background: var(--mantine-color-blue-0, #e7f5ff);
}
.typeCardSelected {
  border: 2.5px solid var(--mantine-color-blue-6, #228be6);
  box-shadow: 0 8px 32px 0 rgba(34, 139, 230, 0.18);
  background: linear-gradient(135deg, #e7f5ff 60%, #f8fafc 100%);
  transform: translateY(-6px) scale(1.04);
  min-width: 220px;
  max-width: 260px;
  min-height: 260px;
  height: 100%;
  box-sizing: border-box;
}
.typeIcon {
  margin-bottom: 0.5rem;
  filter: drop-shadow(0 2px 8px #e7f5ff);
}
.typeTitle {
  font-weight: 700;
  font-size: 1.18rem;
  color: var(--mantine-color-blue-7, #1c7ed6);
  margin-bottom: 0.2rem;
  letter-spacing: 0.01em;
}
.typeDescription {
  color: var(--mantine-color-gray-6, #868e96);
  font-size: 1.01rem;
  font-weight: 400;
  margin-top: 0.1rem;
  line-height: 1.5;
  min-height: 44px;
}
