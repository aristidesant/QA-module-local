#!/bin/sh

# Run typecheck
npm run typecheck

# Run tests only for modified files with coverage check for new files
node scripts/pre-commit-tests.cjs

# Increment version
node scripts/increment-version.cjs

# Add the updated version file to the commit
git add src/version.ts

# Run lint-staged
npx lint-staged
